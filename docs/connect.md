# Connect with Utilint

The maintained integration guide is the [Integrate Utilint skill](../skills/integrate-utilint/SKILL.md).
Give it to your coding agent when adding Utilint to an app. It covers automatic registration,
consent with one callback, per-user tokens, gateway requests, and recovery after client deletion.
